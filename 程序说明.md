# BJKS（本机快搜）- 程序说明

当前版本：**1.0.0**

## 绿色版文件位置

最终生成的绿色版可执行文件为：

```
src-tauri/target/release/BJKS.exe
```

该文件为单文件绿色版，无需安装，双击即可运行。首次启动会自动扫描本地磁盘并建立索引，索引数据保存在 `%LOCALAPPDATA%\本机快搜` 目录下。

## 版本说明

- **1.0.0**（当前版本）
  - 标题栏增加 `?` 按钮，可查看版本号与程序说明
  - 文件列表增加修改时间列
  - 排序支持组合排序（依次点击多个排序按钮）
  - 取消安装包，仅保留绿色版 exe
  - 首次启动优化为后台建立索引，窗口可立即显示

## 项目简介

BJKS 是一款基于 Tauri 2.x + Rust 的 Windows 本地文件快速搜索工具，功能包括：

- 多关键词文件名搜索（空格分隔，命中所有关键词）
- 扩展名过滤与文件类型快速选择（音频、压缩、文档、可执行、图片、视频、文件夹等）
- 文件大小、修改时间排序，支持组合排序
- 右键打开文件/打开所在目录/复制路径
- 屏蔽目录、屏蔽文件模式、屏蔽扩展名设置
- 索引缓存与手动重新扫描

## 必要文件清单

### 前端界面文件

| 文件路径 | 功能说明 |
|---------|---------|
| `src/index.html` | 主界面 HTML 结构，包含搜索框、过滤栏、结果列表、设置面板等 |
| `src/main.js` | 前端交互逻辑：搜索请求、结果渲染、排序、右键菜单、设置面板、文件类型图标映射 |
| `src/styles.css` | 界面样式，定义搜索栏、结果列表、设置面板等视觉效果 |

### Rust 后端文件

| 文件路径 | 功能说明 |
|---------|---------|
| `src-tauri/src/main.rs` | 程序入口，初始化 Tauri 应用、加载索引、注册命令 |
| `src-tauri/src/models.rs` | 数据模型定义：`FileEntry`、`SearchRequest`、`SearchResponse`、`AppConfig`、`IndexStatus` 等 |
| `src-tauri/src/commands/search.rs` | 搜索相关 Tauri 命令：`search`、`get_index_status` |
| `src-tauri/src/commands/settings.rs` | 设置相关 Tauri 命令：配置读写、屏蔽项管理、重新扫描、打开文件/目录 |
| `src-tauri/src/commands/mod.rs` | 命令模块聚合 |
| `src-tauri/src/indexer/startup.rs` | 启动时加载或创建索引，调度扫描与缓存加载 |
| `src-tauri/src/indexer/mft_scanner.rs` | 基于 NTFS MFT / walkdir 的文件扫描实现 |
| `src-tauri/src/indexer/cache.rs` | SQLite 索引缓存，加速后续启动 |
| `src-tauri/src/indexer/mod.rs` | 索引模块聚合、盘符枚举 |
| `src-tauri/src/search/engine.rs` | 搜索引擎核心，执行关键词匹配、扩展名过滤、排序、分页 |
| `src-tauri/src/search/mod.rs` | 搜索模块聚合 |
| `src-tauri/src/logger.rs` | 日志与数据目录定位（`%LOCALAPPDATA%\本机快搜`） |
| `src-tauri/src/config/settings.rs` | 应用配置持久化（JSON 文件） |
| `src-tauri/src/config/mod.rs` | 配置模块聚合 |

### 配置与资源文件

| 文件路径 | 功能说明 |
|---------|---------|
| `src-tauri/tauri.conf.json` | Tauri 主配置文件：窗口定义、安全能力、打包目标、图标引用 |
| `src-tauri/Cargo.toml` | Rust 项目配置与依赖声明 |
| `src-tauri/build.rs` | 构建脚本，声明前端资源变化时重新编译 |
| `src-tauri/capabilities/default.json` | Tauri 2.x 权限能力文件，允许前端调用核心命令 |
| `src-tauri/icons/icon.ico` | 程序图标，用于 exe 文件、窗口标题栏和任务栏 |

## 运行方式

直接双击运行：

```
src-tauri/target/release/BJKS.exe
```

数据目录：

```
%LOCALAPPDATA%\本机快搜
```

包含索引缓存、配置文件和日志文件。

## 重新构建

如果需要重新编译绿色版：

```bash
cd src-tauri
cargo build --release
```

构建产物位于 `src-tauri/target/release/BJKS.exe`。
